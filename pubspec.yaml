name: weather_ios_clone
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  intl: ^0.17.0
  flutter_bloc: ^7.0.1
  flutter_typeahead: ^3.2.1
  cloud_firestore:
  firebase_core:
  get_it: ^7.2.0

  cupertino_icons: ^1.0.2
  location: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - lib/assets/compass.png
    - lib/assets/barometer.png
    - lib/assets/clouds.gif
    - lib/assets/clouds.jpg
    - lib/city.list.json

  uses-material-design: true
